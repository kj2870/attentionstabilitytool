# Research mode build config.
# Usage: copy this to .env.local before building for lab/pilot use.
#   cp .env.research .env.local && npm run build
#
# This produces a build that defaults to research mode.
# Participants can still be switched to consumer mode at runtime if needed.
VITE_APP_MODE=research
